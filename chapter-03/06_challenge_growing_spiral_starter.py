# Challenge: Growing Spiral
# Make a spiral where each forward() step gets longer and the color cycles.
import turtle
t = turtle.Turtle()
t.speed(9)

# Your code here:
# 1) Make a color list (at least 5 colors)
# 2) Loop for ~100 steps
# 3) Increase distance each loop, turn ~89–92 degrees
# 4) Use i % len(colors) to index colors

turtle.done()
