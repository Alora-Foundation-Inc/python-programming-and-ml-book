# Power‑Ups Mode — Starter
import turtle, time, random

screen = turtle.Screen(); screen.setup(600,600); screen.bgcolor("black"); screen.tracer(0)

# TODO:
# - Add timed power‑ups (slow time, invincible, double points)
# - Show countdown timer for active effect
# - Make power‑ups appear randomly and expire if not collected
screen.exitonclick()
