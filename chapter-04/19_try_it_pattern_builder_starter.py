# Try-It: draw_pattern(kind, repeats=10, size=50, color_list=None)
# - If kind == "star": draw N stars around a circle
# - If kind == "spiral": draw a spiral with N steps
# - If kind == "flowers": place N flowers in a ring

import turtle
t = turtle.Turtle()
t.speed(9)

# TODO: implement draw_pattern with if/elif branches

turtle.done()
