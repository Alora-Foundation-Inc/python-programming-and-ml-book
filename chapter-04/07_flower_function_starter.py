import turtle

my_turtle = turtle.Turtle()
my_turtle.speed(8)

# TODO: Create a function draw_flower(petals, size, color)
# Hints:
# - Each petal can be made with two arcs (turtle.circle(radius, extent))
# - Turn by 360/petals between petals

# Then call your function a few times with different parameters.

turtle.done()
