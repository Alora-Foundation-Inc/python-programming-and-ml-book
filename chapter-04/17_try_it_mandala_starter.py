# Try-It: draw_mandala(spokes=12, radius=80, color_list=None)
# - Use a loop that rotates 360/spokes each time
# - Draw a small motif per spoke (e.g., short line + tiny triangle)

import turtle
t = turtle.Turtle()
t.speed(0)

# TODO: define draw_mandala(...)
# TODO: call it with a few different parameters

turtle.done()
