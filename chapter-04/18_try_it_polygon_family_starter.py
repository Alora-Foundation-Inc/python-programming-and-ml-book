# Try-It: make small wrapper functions like triangle(), square(), hexagon()
# All should call one base function draw_polygon(sides, size, color).

import turtle
t = turtle.Turtle()
t.speed(7)

# TODO: implement draw_polygon and the wrappers
# triangle(size, color), square(size, color), pentagon(size, color), ...

turtle.done()
